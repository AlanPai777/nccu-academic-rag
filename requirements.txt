# Web crawling & HTML parsing
httpx
beautifulsoup4
lxml

# Document parsing
pdfplumber

# RAG: LlamaIndex (retriever layer — dense + keyword + RRF fusion)
llama-index-core
llama-index-embeddings-ollama
llama-index-vector-stores-qdrant

# RAG: Vector database
qdrant-client

# RAG: Keyword search (FTS5 Chinese segmentation)
jieba

# RAG: Token counting (PDF chunker)
tiktoken

# Web UI
gradio

# Agentic RAG (Prototype 1+)
ollama
openai>=1.0
python-dotenv>=1.0

# Agentic RAG (Prototype 3 — LangGraph)
langgraph

# Note: PyTorch is NOT listed here — install separately based on your hardware:
#
# CPU only:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# NVIDIA GPU (CUDA):
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#
# Intel GPU (Arc / Core Ultra iGPU):
#   Use ipex-llm Ollama for LLM inference + standalone Ollama for embedding.
#   See: https://github.com/intel/ipex-llm
#
# Removed:
#   sentence-transformers / einops — Jina reranker replaced by RRF fusion (Phase 5.5)
#   python-docx / openpyxl — RAG pipeline handles HTML and PDF only
#   langdetect — not used in current pipeline
